"""`SparseFluxObjective`: the sparse ``J(v)`` and its ``(v, z)`` LP with ``z = |v|``
auxiliary variables assembled directly in CSC. ``z`` is LP-only and is never sampled.

Implemented in **M3** — see BUILD_PLAN.md.
"""

from __future__ import annotations
