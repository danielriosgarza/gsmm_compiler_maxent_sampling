"""Downstream feature extraction (raw / activity / exchange / pathway).

Flux thresholds live **here only** — chain state is never snapped to zero.

Implemented in **M8** — see BUILD_PLAN.md.
"""

from __future__ import annotations
